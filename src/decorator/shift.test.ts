import { shiftX, shiftY } from "./shift";
import { describe, it, expect } from "vitest";

describe("Decorator - shift", () => {
  shiftX;
  shiftY;
  it("TBD", () => {
    expect(true).toBeTruthy();
  });
});
